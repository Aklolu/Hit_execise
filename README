Assumption is that mgr host does not need a username and password as authentication is catered for to access n1&n2 as not provided
IP shows all hosts are in the same class C network
Created 2 functions
1 function to for getting and sorting files as well as metadata
second function is to call the function with the 2 file paths passed twice in parallel then compare the results which are returned in arra
